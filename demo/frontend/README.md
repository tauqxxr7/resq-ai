# Historical starter — not the supported demo

This untouched create-next-app starter is retained to preserve existing work. It does not contain the incident analyst and is not included in root build/test commands.

Use [`../../frontend`](../../frontend) and run `npm run demo` from the [repository root](../../README.md). Deploy `frontend`, never `demo/frontend`.
