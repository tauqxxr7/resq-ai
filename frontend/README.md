# Supported ResQ application

This directory contains the Next.js UI and server-side analysis API. Follow the [root quickstart](../README.md).

From repository root: `npm run demo`. Standard production commands remain `npm run build` and `npm --prefix frontend start`.

Environment template: [`.env.example`](.env.example). Never put AWS credentials in `NEXT_PUBLIC_*` variables.
