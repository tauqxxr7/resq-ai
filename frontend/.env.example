# Optional. Default is deterministic demo; npm run demo always forces demo mode.
RESQ_PROVIDER=demo
# To use Bedrock, set RESQ_PROVIDER=bedrock and choose an accessible region/model.
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=
# Use AWS_PROFILE or your host's IAM role / default SDK credential chain.
# Never put AWS credentials in NEXT_PUBLIC_* variables or commit them.
